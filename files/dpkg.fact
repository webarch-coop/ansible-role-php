#!/usr/bin/env bash
# Ansible managed

set -euo pipefail
DPKG="/usr/bin/dpkg"

if [[ -f "${DPKG}" ]]; then
  ARCH=$("${DPKG}" --print-architecture)
  jo state=present arch="${ARCH}"
else
  jo state=absent
fi
